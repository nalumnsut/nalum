{
  "scripts": {
    "dev:up": "docker compose -f docker-compose.yml -f docker-compose.override.yml up -d --build backend redis mongo postgres",
    "dev:up:all": "docker compose -f docker-compose.yml -f docker-compose.override.yml up -d --build",
    "dev:down": "docker compose -f docker-compose.yml -f docker-compose.override.yml down",
    "dev:seed": "docker compose exec backend node scripts/seed/index.js"
  },
  "dependencies": {
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "tslib": "^2.8.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.2",
    "vite": "^7.3.0"
  }
}
